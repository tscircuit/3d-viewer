import type { AnySoupElement } from "@tscircuit/soup"

export const useConvertChildrenToSoup = (
  children?: any,
  initialSoup?: AnySoupElement[]
) => {
  // TODO convert children if defined

  return initialSoup!
}
